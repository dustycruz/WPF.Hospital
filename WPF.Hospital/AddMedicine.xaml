<Window x:Class="WPF.Hospital.AddMedicine"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="AddMedicine" Height="200" Width="450">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Content="Add New Medicine" FontWeight="Bold"></Label>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>

                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Content="Medicine Name"></Label>
            <TextBox Grid.Column="1" Text="{Binding MedicineName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label Grid.Row="1" Content="Brand"></Label>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            
            <Button x:Name="btnAddMedicine" Grid.Row="4" Grid.ColumnSpan="2" Content="Add Medicine" Click="btnAddMedicine_Click"></Button>
        </Grid>
    </Grid>
</Window>
