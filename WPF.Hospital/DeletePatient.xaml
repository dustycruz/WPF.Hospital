<Window x:Class="WPF.Hospital.DeletePatient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="DeletePatient" Height="170" Width="400">
    <Grid Margin="5">


        
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        
        <Label Content="Delete New Patient" FontWeight="Bold"></Label>
        
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>

                <RowDefinition></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>

            </Grid.RowDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Content="Id" FontWeight="Bold"></Label>


                <TextBox Grid.Row="1" x:Name="tbPatientId" Height="30" Grid.Column="0" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"></TextBox>

            </Grid>
            <Button x:Name="btnDeletePatient" Grid.Row="1" Grid.ColumnSpan="2" Content="Delete Patient" Click="btnDeletePatient_Click"></Button>
        </Grid>
    </Grid>
</Window>
